## 📊 Pandas in AI & Machine Learning

**Pandas** is a Python library used for **data manipulation and analysis**.  
In AI and Machine Learning, Pandas is mainly used to **load, clean, explore, and prepare data** before training models.

---

### Why Pandas is Important in AI/ML
- Real-world data is **raw and messy**
- ML models need **clean and structured data**
- Pandas helps convert raw data into **model-ready datasets**

---

### Common Uses of Pandas in AI/ML

- 📥 **Loading data**  
  Read data from CSV, Excel, JSON, databases, etc.
  ```python
  import pandas as pd
  df = pd.read_csv("data.csv")
